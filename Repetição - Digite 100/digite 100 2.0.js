
// Criar um programa onde que deve pedir repetidamenete ao usuário digitar um número,
// até que o número digitado seja 100

var num = 0
var nome = ""

while (num != 100 || nome != "Alfredo") {

    nome = prompt("Digite um nome:")
    num = Number(prompt ("Digite um número:"))
}